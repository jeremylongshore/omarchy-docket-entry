{
  "schemaVersion": 1,
  "id": "io.github.jeremylongshore.docket",
  "name": "Docket",
  "version": "1.1.0",
  "author": "Jeremy Longshore",
  "license": "MIT",
  "description": "Docket turns GitHub pull requests into an obligation queue for action, not scrolling. It separates requests waiting on your review, your own blocked work, and branches ready to merge, then sorts lanes oldest first. A configurable review clock marks overdue work before it disappears in the feed. Drain a row when handled; it returns on its own when the pull request changes. One bounded GraphQL poll every 15 minutes keeps the bar quiet while credentials stay local and never enter process arguments.",
  "kinds": [
    "service",
    "bar-widget"
  ],
  "entryPoints": {
    "service": "Service.qml",
    "barWidget": "BarWidget.qml"
  },
  "barWidget": {
    "displayName": "Docket",
    "description": "Docket turns GitHub pull requests into an obligation queue for action, not scrolling. It separates requests waiting on your review, your own blocked work, and branches ready to merge, then sorts lanes oldest first. A configurable review clock marks overdue work before it disappears in the feed. Drain a row when handled; it returns on its own when the pull request changes. One bounded GraphQL poll every 15 minutes keeps the bar quiet while credentials stay local and never enter process arguments.",
    "category": "Developer Tools",
    "aliases": [
      "docket",
      "pull-requests",
      "review-queue",
      "code-review",
      "github-prs"
    ],
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "notifications": "On",
      "slaHours": 24,
      "botPullRequests": "Hide",
      "myDrafts": "Hide"
    },
    "schema": [
      {
        "key": "notifications",
        "type": "enum",
        "label": "Desktop notifications",
        "options": [
          "On",
          "Off"
        ],
        "defaultValue": "On",
        "description": "Notify when a pull request newly lands on your docket. Never on the first run, never on an error, never on something you already dismissed."
      },
      {
        "key": "slaHours",
        "type": "integer",
        "label": "Review clock (hours)",
        "min": 1,
        "max": 168,
        "step": 1,
        "defaultValue": 24,
        "description": "How long a pull request may sit on your docket before it counts as overdue. Overdue rows carry a marker and turn the pill to the bar alert color."
      },
      {
        "key": "botPullRequests",
        "type": "enum",
        "label": "Bot-authored pull requests",
        "options": [
          "Hide",
          "Show"
        ],
        "defaultValue": "Hide",
        "description": "Dependency bots open more review requests than people do and never age out. Hidden by default so the queue reflects human obligation."
      },
      {
        "key": "myDrafts",
        "type": "enum",
        "label": "Your own drafts",
        "options": [
          "Hide",
          "Show"
        ],
        "defaultValue": "Hide",
        "description": "A draft is waiting on you by definition, so showing them drowns the real signal. Hidden by default."
      }
    ]
  }
}
